# ML / Fine-tuning пакеты для Спринта 8 (QLoRA адаптеры)
# Устанавливать отдельно: pip install -r requirements/ml.txt
#
# ВАЖНО: эти пакеты конфликтуют с chromadb по версии tokenizers.
# Устанавливать в отдельное окружение или после завершения RAG-разработки.

transformers>=4.44.0,<4.46.0     # 4.44.x совместим с tokenizers~=0.19
peft>=0.12.0
bitsandbytes>=0.43.0
torch>=2.4.0
langchain-huggingface>=0.1.0
